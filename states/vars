{% set user = 'roy' %}
{% set homeDir = salt['environ.get']('HOME') %}
{% set statePath = '/salt/states' %}

{% set vars = {
  'user' : user,
  'homeDir' : homeDir,
  'statePath' : homeDir + statePath
} %}
